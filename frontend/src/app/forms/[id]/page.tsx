'use client';

// Reuses the same form builder but in edit mode
// The form builder at /forms/new also handles /forms/[id] via params.id
import FormBuilderPage from '../new/page';
export default FormBuilderPage;
